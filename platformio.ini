; Workspace bootstrap only. Real PlatformIO projects live under projects/.

[platformio]
lib_dir = projects

[env:workspace-bootstrap]
platform = native
extra_scripts = pre:tools/platformio/pio-workspace-bootstrap.py
build_src_filter = -<*>
lib_ignore = *
